# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- no-render

pool: Default
jobs:
- job: 'EmptyRooms00025'
  strategy:
    parallel: 5
  timeoutInMinutes: 0 # how long to run the job before automatically cancelling
  steps:
  - script: |
      $(System.DefaultWorkingDirectory)
      cd /home/selab1421/Desktop/code/stable-baselines
      git checkout reward-correlation-0.0025
      SB_PATH=/home/selab1421/Desktop/code/stable-baselines ./scripts/run_docker_gpu.sh python examples/EmptyRooms/PPO2/empty_map/run_base_env.py 
    displayName: 'Training'
